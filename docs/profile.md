# پروندهٔ این پروژه — Project profile

> این فایل را دستیار در گامِ `/start` پر می‌کند و در گامِ `/deploy` کاملش می‌کند. تو لازم نیست دستش بزنی.
>
> The assistant fills this in at `/start` and completes it at `/deploy`. You do not need to edit it.

| کلید | مقدار |
|---|---|
| زبانِ گفت‌وگو — conversation | English |
| زبانِ مستندات — documents | فارسی |
| نامِ پروژه — project | Size |
| انبارِ کد — repo | https://github.com/nepticoo/size_bot |
| پورتِ محلی — local port | 8000 |
| آدرسِ محلی — local address | http://localhost:8000 |
| روشِ استقرار — deploy method | <!-- پیش‌فرض / سفارشی — در /deploy تعیین می‌شود --> |
| آدرسِ عمومیِ محصول — public address | <!-- بعد از /deploy پر می‌شود --> |

> پورت هیچ‌جا از قبل تعیین نشده است. پیش‌فرض `8000` است و اگر روی این کامپیوتر اشغال بود، دستیار اولین پورتِ آزاد را برمی‌دارد و همین‌جا و در `.env` می‌نویسد.
