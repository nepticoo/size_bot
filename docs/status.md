# وضعیتِ پروژه و چک‌لیستِ پیشرفت — Project Status & Checklist

> **دستیار:** این فایل سندِ زنده و فایلِ انتقال (Hand-off) بین نشست‌ها و دستیارهاست.
> بعد از هر قدمِ معنادار، چک‌لیست، تعداد دورها (Rounds) و دستوراتِ شناور (مثل `/add-idea`) را به‌روز کن. اگر توکنِ دستیار تمام شد یا نشست قطع گردید، دستیارِ بعدی فقط با خواندنِ همین فایل کار را بی‌وقفه ادامه می‌دهد. بخش‌ها را کوتاه و واقعی نگه دار.
>
> **تو:** لازم نیست این فایل را ویرایش کنی؛ این‌طوری دستیارها هماهنگ می‌مانند.

## اطلاعاتِ پایه
- **نام پروژه:** Size
- **زبان گفت‌وگو:** انگلیسی
- **زبان مستندات:** فارسی
- **آدرس محصول:** محلی (`http://localhost:8000`) / مستقر (پس از `/deploy`)

## چک‌لیستِ مراحل اصلی (Core Phases)

### روز اول — طراحی و مستندات
- [x] `/start` — راه‌اندازی، زبان، پروفایل و تست گیت — کامل شد؛ گیت نصب و مخزن متصل است
- [x] `/idea-core` — هستهٔ ایده، تحلیل رقبا، هرس اسکوپ و وابستگی‌ها (دورها: ۲ — بسته شد، ۱۹ تصمیم ثبت شد)
- [x] `/prd` — سند محصول و ارزش ویژه (دورها: ۳ — بسته شد، ۳۱ تصمیم ثبت شد)
- [x] `/data-model` — مدل داده و چیزهایی که سیستم نگه می‌دارد (دورها: ۱ — بسته شد، ۳۵ تصمیم ثبت شد)
- [x] `/ux` — ساختار صفحات، جریان کاربر و قالب (دورها: ۱ — بسته شد، ۴۰ تصمیم ثبت شد)
- [x] `/design` — تولید پروتوتایپ و صفحات بصری در UI/ (دورها: ۳ — بسته شد، ۴۴ تصمیم ثبت شد)

### روز دوم — ساخت و استقرار
- [x] `/reconcile` — تطبیق و هم‌راستاسازی اسناد پس از یک هفته (دورها: ۱ — بسته شد، ۴۸ تصمیم ثبت شد)
- [x] `/deps` — آماده‌سازی سرویس‌ها و ثبت رمزها در secrets.local.md (هیچ وابستگیِ بیرونی‌ای نبود — بدونِ کارِ کاربر بسته شد)
- [x] `/scenarios` — سناریوهای تست قبولی (دورها: ۱ — بسته شد، ۲۸ سناریو، ۵۲ تصمیم ثبت شد)
- [x] `/plan` — معماری، چک‌لیست ساخت و هندآف (۵۷ قدم، هر ۲۸ سناریو پوشش داده شد)
- [x] `/setup-dev` — نصب و تأیید ابزارهای توسعه روی سیستم (Python 3.14 و Node 26 از قبل نصب بودند؛ اسنادِ پین‌شده به‌روز شدند)
- [ ] `/build` — کدنویسی، ساخت برنامه و پاس شدن تست‌ها در لوکال
- [ ] `/deploy` — انتشار عمومی روی اینترنت و تأیید آدرس زنده
- [ ] `/test` — تست‌های پذیرش نهایی همراه با بنیان‌گذار (دورها: ۰)
- [ ] `/iterate` — چرخهٔ نسخه‌های بعدی (0.2+)

## رویدادها و دستورات شناور (Floating Steps Log)
<!-- هر دستور شناور مانند /add-idea با گام جاری ثبت می‌شود -->
- ۲۰۲۶-۰۸-۲۹ — به واقع `/start` اجرا شد: زبان‌ها انتخاب شدند (گفت‌وگو انگلیسی، مستندات فارسی)، پروفایلِ پروژه پر شد، و پورتِ محلی ۸۰۰۰ بررسی و آزاد تشخیص داده شد.
- ۲۰۲۶-۰۸-۲۹ — مانع: گیت روی این کامپیوتر نصب نیست و این پوشه هنوز یک مخزنِ کد نیست. از کاربر خواسته شد گیت را نصب کند و پوشه را روی یک مخزنِ **خصوصی** در گیت‌هابِ خودش منتشر کند.
- ۲۰۲۶-۰۹-۰۵ — **مخزن جابه‌جا شد.** مخزنِ اولیه دیگر پیدا نمی‌شد و پوش رد می‌شد. کاربر مخزنِ خصوصیِ تازه‌ای روی حسابِ `nepticoo` به نامِ `size_bot` ساخت و یک کلیدِ SSH (Deploy key، با اجازهٔ نوشتن) برایش ثبت کرد. آدرسِ `origin` باید روی `git@github.com:nepticoo/size_bot.git` باشد.
- ۲۰۲۶-۰۹-۰۵ — به واقع `/setup-dev` اجرا شد. پایه از قبل روی سیستم بود: پایتون ۳.۱۴.۲ (نه ۳.۱۲)، Node v26.3.1 (نه ۲۲ LTS)، npm ۱۱.۱۶.۰، گیت ۲.۵۰.۱. چون نصبِ نسخه‌های پین‌شده (winget) رد شد، تصمیم شد با نسخه‌های موجود جلو برویم و سندِ `02-tech-stack.md` و بخشِ «چه چیزی باید نصب باشد» در `architecture.md` به همین نسخه‌ها به‌روز شدند. سازگاریِ بسته‌های حساس بررسی شد: `opencv-python-headless`، `pillow-heif`، `numpy`، `Pillow` همه چرخِ (wheel) آماده برای پایتونِ ۳.۱۴ روی ویندوز دارند (بررسی با `pip install --dry-run`). اثباتِ کارکرد: `python -m venv` در پوشهٔ اسکرچ ساخته شد و پاک شد، `npm --version` داخلِ پوشهٔ پروژه جواب داد.
- ۲۰۲۶-۰۸-۲۹ — **مانع برطرف شد.** گیت نصب شد (نسخهٔ ۲.۵۵)، کاربر پوشه را روی `https://github.com/habibinavid404-star/Size` منتشر کرد و کارِ تا اینجا را ذخیره کرد. نام و ایمیلِ گیت تنظیم است، شاخه `main` است، و بررسی شد که `.env` و `secrets/secrets.local.md` هیچ‌کدام ردیابی نمی‌شوند. **تنها چیزی که ماشین نمی‌تواند بررسی کند خصوصی بودنِ مخزن است؛ از کاربر خواسته شد یک‌بار در مرورگر تأییدش کند.**

## وضعیت جاری و قدمِ بعدی (Hand-off State)
- **مرحلهٔ فعال:** به واقع `/setup-dev` بسته شد. مرحلهٔ بعدی `/build` است، در یک نشستِ تازه.
- **همین حالا در دستِ کار:** هیچ کارِ نیمه‌تمامی نیست. `/plan` بسته شد: `architecture.md` (جدول‌ها، اندپوینت‌ها، خطِ لولهٔ اندازه‌گیری، الگوریتمِ سایز، و بخشِ «چه چیزی باید نصب باشد» که `/setup-dev` از رویش نصب می‌کند)، `checklist.md` با ۵۷ قدم که هر ۲۸ سناریو را پوشش می‌دهد، و `handoff.md` برای نشستی که هیچ‌کدامِ این گفت‌وگو را ندیده. `/scenarios` بسته شد: بیست‌وهشت سناریو با دادهٔ مشخص در `docs/living/test-scenarios.md` (کپیِ نسخه‌ای در `12-scenarios.md`). نوشتنشان چهار سوراخ را لو داد که همه جواب گرفتند (تصمیم‌های ۴۹ تا ۵۲، همه با پیش‌فرض) — دوتاشان جدی بودند و مدلِ داده را هم عوض کردند. `/deps` بسته شد و **هیچ کاری برای کاربر نداشت** — نسخهٔ ۰.۱ هیچ سرویسِ بیرونی صدا نمی‌زند، پس نه ثبت‌نامی لازم بود نه کلیدی. پله‌های جایگزینِ هر پنج حوزه از هستهٔ ایده به `dependencies.md` منتقل شدند تا اگر روزی چیزی نخواند، همان‌جا که کاربر نگاه می‌کند نوشته باشد. `SECRET_KEY` و ورودِ گرداننده را دستیار در `/setup-dev` می‌سازد. `/reconcile` در یک دور بسته شد: هیچ فایلِ تازه‌ای از طولِ هفته نبود، و مقایسهٔ طرح با `prd.md` و `data-model.md` چهار قولِ بی‌صفحه پیدا کرد که هر چهار جواب گرفتند (تصمیم‌های ۴۵ تا ۴۸، همه با پیش‌فرض). `prd.md` و `ux.md` به‌روز شدند و `dependencies.md` پر شد. نُه دورِ سؤال (`02` تا `10`) بسته شده و ۴۸ تصمیم ثبت شده است. طراحی با **Claude on the web (بخش Design)** انجام شد و خروجی در `UI/Screens v2.dc.html` است — هر چهارده صفحه با حالت‌های خطا و خالی‌شان. سه دورِ طراحی در `design-chat.md` ثبت شده. تطبیقِ طرح با سندها انجام شد، چهار ناهماهنگی در `09-questions.md` پیدا شد و هر چهار جواب گرفت (تصمیم‌های ۴۱ تا ۴۴، همه با پیش‌فرض). `ux.md` به‌روز شد. هشت دورِ سؤال (`02` تا `09`) بسته شده و ۴۴ تصمیم ثبت شده است.
- **قدمِ بعدیِ دستیار:** `/setup-dev` (در پنلِ Copilot، نه اینجا) و بعد `/build` در یک نشستِ تازه با Sonnet و تلاشِ متوسط. نشستِ بعدی **فقط** به سه فایل نیاز دارد: `docs/versions/0.1/handoff.md` را اول بخواند، بعد `checklist.md`، بعد `docs/living/architecture.md`. هیچ چیزی از این گفت‌وگو لازم نیست.
- **چهار سؤالِ باز (`13-questions.md`) ساخت را متوقف نمی‌کنند** — پیش‌فرض‌هایشان در `architecture.md` نوشته شده و ساخت با همان‌ها جلو می‌رود. اگر کاربر جوابِ دیگری داد، چند عددِ ثابت در `sizing/compare.py` عوض می‌شود.
- **هفت چیزی که در سند هست ولی در طرح کشیده نشده.** طرحی برایشان لازم نیست؛ در `/plan` و `/build` ساخته می‌شوند و اینجا نوشته‌اند تا فراموش نشوند: چیدمانِ صفحهٔ آغاز بدونِ عکسِ محصول · دکمهٔ «خروج» در نوارِ بالای پنل · صفحهٔ جواب بدونِ دکمهٔ «برگرد و خرید کن» · دکمهٔ «کپیِ لینکِ این جواب» · نوارِ «داری به‌جای فلانی کار می‌کنی» و «ورود به پنلِ این فروشگاه» · «غیرفعال کردنِ این محصول» و غیرفعال کردنِ فروشگاه · جدا شدنِ خطِ اختلافِ طول از نکته‌های فرعی.
- **مانع‌ها / منتظرِ چه چیزی:** روزِ دوم هفتهٔ آینده با `/reconcile` شروع می‌شود. **مخزن عوض شد:** مخزنِ قدیمی (`habibinavid404-star/Size`) دیگر در دسترس نبود. کاربر مخزنِ خصوصیِ تازه‌ای روی حسابِ دیگری ساخت — `nepticoo/size_bot` — و برایش کلیدِ SSH از نوعِ Deploy key با اجازهٔ نوشتن ثبت کرد. کلید در `~/.ssh/id_ed25519` است و کار می‌کند (با `ssh -T git@github.com` بررسی شد).
- **چیزها کجا هستند:** طرحِ بصری `UI/Screens v2.dc.html` است (در مرورگر باز می‌شود؛ فایلِ `support.js` که صدا می‌زند فقط ابزارِ بزرگ‌نماییِ خودِ Claude است و نبودنش اشکالی ندارد). پوشهٔ نسخهٔ جاری `docs/versions/0.1/` است. مخزنِ کد `https://github.com/nepticoo/size_bot` است و از طریقِ SSH وصل می‌شود (`git@github.com:nepticoo/size_bot.git`). محصول فعلاً هیچ‌جا اجرا نمی‌شود. فایلِ رمزها `secrets/secrets.local.md` هنوز خالی است و در ۰.۱ فقط دو مقدار خواهد داشت که دستیار در `/setup-dev` می‌سازد. نسخهٔ ۰.۱ هیچ وابستگیِ بیرونی ندارد.

## خلاصهٔ تصمیم‌های ساختاریِ نسخهٔ ۰.۱
- محصول یک **صفحهٔ خریدار با دو جای نصب** است: لینک برای اینستاگرام (مسیرِ اصلی) و یک قطعهٔ چسباندنی برای سایتِ فروشنده. اتصالِ مستقیم به دایرکتِ اینستاگرام در کار نیست — اجازه‌اش هفته‌ها طول می‌کشد و در دو روز شدنی نیست.
- **عکسِ خریدار بعد از ۳۰ دقیقه پاک می‌شود؛ اندازه‌های عددی می‌مانند.** این هم یک وعدهٔ فروش است و هم هزینهٔ نگهداری را نزدیکِ صفر نگه می‌دارد. فروشنده هم عکس را نمی‌بیند. تا پایانِ همان نیم‌ساعت، خریدار با یک لینکِ کوتاه‌مدت می‌تواند جوابش را دوباره ببیند.
- **برندِ دوگانه:** نامِ فروشگاه بالای صفحهٔ خریدار، خطِ کوچکِ «قدرت‌گرفته از سایز» پایین.
- **جدولِ سایز چندمعیاره و توسعه‌پذیر است.** همهٔ معیارها سنجیده و گزارش می‌شوند، ولی وزنِ تصمیم فقط با دو ملاکِ اصلی است: بالاتنه دورِ سینه و طولِ لباس، پایین‌تنه دورِ کمر و قد. اگر آن دو نخوانند، دور حرفِ آخر را می‌زند و اختلافِ طول صریح گفته می‌شود. **معیارها باید داده باشند نه ستونِ ثابتِ کد.**
- دیتای جدول را هم فروشنده و هم گردانندهٔ سیستم می‌تواند وارد کند. معیارِ تازه را فقط گرداننده اضافه می‌کند و درخواستش دستی می‌رسد — هیچ صفحه‌ای برایش ساخته نمی‌شود.
- **قاعدهٔ «دور» در برابر «عرض»:** برای هر محصول جدا انتخاب می‌شود (با پیش‌پر شدن از انتخابِ قبلیِ همان فروشگاه) و سیستم تبدیل می‌کند. حساس‌ترین نقطهٔ محصول — اشتباه گرفتنشان یعنی جوابِ دو برابر غلط با اطمینانِ کامل.
- **حذفِ محصول توسطِ فروشنده یعنی غیرفعال شدن، نه پاک شدن.** اندازه‌های بی‌نام می‌مانند؛ حذفِ واقعی فقط دستِ گرداننده است.
- **لینکِ محصول و لینکِ دیدنِ دوبارهٔ جواب باید حدس‌ناپذیر باشند** — نه شمارهٔ پشتِ سرِ هم، وگرنه با عوض کردنِ یک رقم می‌شود به فروشگاه‌ها و جواب‌های دیگر رسید.
- **عددِ دقیقِ اندازه به خریدار گفته نمی‌شود** — به‌جایش مقایسهٔ نسبی با لباسِ خودش. عدد یک ادعای قابلِ سنجش می‌سازد و تا وقتی دقت روی فروشگاه‌های واقعی اثبات نشده، ریسکِ بی‌فایده است.
- **شکلِ محصول:** صفحهٔ خریدار فقط گوشی؛ پنلِ فروشنده گوشی‌اول ولی روی کامپیوتر هم مرتب.
- **تم:** مطمئن، ساده، بی‌ادعا. پایهٔ سفید با متنِ تیره و یک رنگِ تأکیدیِ آبیِ آرام و کمی تیره. گوشه‌های نرم. صفحه‌های خریدار خیلی خلوت، صفحه‌های فروشنده کاری و فشرده‌تر. همه‌جا راست‌به‌چپ.
- **چهارده صفحهٔ نام‌گذاری‌شده** در `ux.md`: شش‌تا خریدار، شش‌تا فروشنده، دوتا گرداننده. سناریوهای تست و طراحی با همین نام‌ها صدایشان می‌زنند.
- اندازه‌گیری **روی کامپیوترِ خودِ کاربر** انجام می‌شود، بدونِ هیچ سرویسِ هوشِ مصنوعیِ بیرونی — هم وابستگی ندارد، هم برای اندازه‌گیریِ عددی دقیق‌تر است.
- خریدار حساب نمی‌سازد. فروشنده با نام کاربری و رمز وارد می‌شود. **پیامکِ تأیید در کار نیست.**
- در نسخهٔ ۰.۱ هیچ پرداختی داخلِ محصول نیست؛ تسویه بیرون از سیستم.
- وعدهٔ دقتِ «یک‌دهمِ سانتی‌متر» واقع‌بینانه نیست؛ دقتِ واقعی حدودِ نیم سانتی‌متر است که چند برابر ریزتر از فاصلهٔ دو سایزِ متوالی است و برای جوابِ درست کاملاً کافی است.
